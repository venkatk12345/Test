version: 2
sources:
  - name: datafeed_shared_schema
    database: "{{('database_test')}}"
    schema: test

    tables:
      - name: RAW_CUSTOMERS
      - name: RAW_PAYMENTS
      - name: RAW_ORDERS
