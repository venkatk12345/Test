version: 2

exposures:

  - name: weekly_metrics
    label: saisri by the Week
    type: dashboard
    maturity: high
    description: 
     For more exposure about project details

    depends_on:
      - ref('order_append')
      - ref('order_merge')
      - ref('order_macro_data')
      - ref('order_delete_insert')
      - source('datafeed_shared_schema','RAW_CUSTOMERS')
      - source('datafeed_shared_schema','RAW_PAYMENTS')
      - source('datafeed_shared_schema','RAW_PRODUCTS')
  
    owner:
      name:  venkat
      email: venkatk.dxc@gmail.com
